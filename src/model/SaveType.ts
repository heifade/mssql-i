/**
 * 保存枚举
 *
 * @export
 * @enum {number}
 */
export enum SaveType {
  /**
   * 插入
   */
  insert = 1,
  /**
   * 更新
   */
  update = 2,
  /**
   * 删除
   */
  delete = 3,
  /**
   * 替换
   */
  replace = 4,
}
